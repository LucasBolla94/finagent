# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic[email]==2.9.2
pydantic-settings==2.5.2

# Database
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.3
pgvector==0.3.3

# Cache & Queue
redis==5.1.1
celery==5.4.0
celery[redis]

# AI & HTTP
httpx==0.27.2
openai==1.51.2        # OpenRouter is compatible with OpenAI SDK

# Document Processing
pdfplumber==0.11.4    # Digital PDF text extraction
pymupdf==1.24.11      # Fast PDF to image conversion (for Vision AI)
Pillow==10.4.0        # Image processing

# Auth
python-jose[cryptography]==3.3.0
# BUG-003 FIX: passlib 1.7.4 is incompatible with bcrypt>=4.1 (removed __about__).
# Pin bcrypt==4.0.1 — last version that passlib supports without breaking.
# Without this pin, pip resolves bcrypt>=4.1 and Register/Login crash with 500.
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.12

# WhatsApp
pywa==2.4.0           # python-whatsapp-bot (Evolution API compatible)

# Telegram
python-telegram-bot==21.6

# Utils
python-dotenv==1.0.1
structlog==24.4.0
sentry-sdk[fastapi]==2.14.0

# Dev only
pytest==8.3.3
pytest-asyncio==0.24.0
httpx                  # for testing
